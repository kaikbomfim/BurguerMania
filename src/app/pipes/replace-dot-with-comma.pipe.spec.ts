import { ReplaceDotWithCommaPipe } from "./replace-dot-with-comma.pipe";


describe('ReplaceDotWithCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceDotWithCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
